{
  "name": "nos-timing",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build index.ts --compile --outfile timing-export",
    "run": "bun run index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.3.3"
  }
}